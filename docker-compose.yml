version: '3.8'

services:
  bot:
    build: .
    container_name: bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    volumes:
      - ./data:/app/data
    ports:
      - "8080:8080" 